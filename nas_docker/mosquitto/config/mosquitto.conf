# Mosquitto MQTT Broker config
listener 1883
allow_anonymous true

# Persist messages across restarts
persistence true
persistence_location /mosquitto/data/

# Log to stdout (visible in Container Manager logs)
log_dest stdout
log_type all
